@{
    ViewData["Title"] = "Đặt hàng thành công";
    string orderNumber = ViewBag.OrderNumber ?? "TCS" + DateTime.Now.ToString("yyyyMMddHHmmss");
    DateTime estimatedDelivery = DateTime.Now.AddDays(3);
}

<div class="thank-you-container">
    <div class="thank-you-card">
        <div class="thank-you-header">
            <div class="success-icon">
                <i class="fas fa-check-circle"></i>
            </div>
            <h1 class="thank-you-title">Cảm ơn bạn đã đặt hàng!</h1>
            <p class="thank-you-subtitle">Đơn hàng của bạn đã được xác nhận và đang được xử lý</p>
        </div>

        <div class="order-details">
            <div class="order-info">
                <div class="info-item">
                    <span class="info-label">Mã đơn hàng:</span>
                    <span class="info-value">@orderNumber</span>
                </div>
                <div class="info-item">
                    <span class="info-label">Ngày đặt hàng:</span>
                    <span class="info-value">@DateTime.Now.ToString("dd/MM/yyyy HH:mm")</span>
                </div>
                <div class="info-item">
                    <span class="info-label">Dự kiến giao hàng:</span>
                    <span class="info-value">@estimatedDelivery.ToString("dd/MM/yyyy")</span>
                </div>
            </div>

            <div class="delivery-status">
                <div class="status-timeline">
                    <div class="status-step active">
                        <div class="step-icon">
                            <i class="fas fa-clipboard-check"></i>
                        </div>
                        <div class="step-label">Đã xác nhận</div>
                    </div>
                    <div class="status-step">
                        <div class="step-icon">
                            <i class="fas fa-box"></i>
                        </div>
                        <div class="step-label">Đang chuẩn bị</div>
                    </div>
                    <div class="status-step">
                        <div class="step-icon">
                            <i class="fas fa-shipping-fast"></i>
                        </div>
                        <div class="step-label">Đang giao hàng</div>
                    </div>
                    <div class="status-step">
                        <div class="step-icon">
                            <i class="fas fa-home"></i>
                        </div>
                        <div class="step-label">Đã giao hàng</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="thank-you-message">
            <p>Chúng tôi đã gửi email xác nhận đơn hàng đến địa chỉ email của bạn. Bạn có thể theo dõi trạng thái đơn hàng trong tài khoản của mình.</p>
            <p>Nếu bạn có bất kỳ câu hỏi nào về đơn hàng, vui lòng liên hệ với chúng tôi qua email <a href="mailto:support@thecoffeeshop.com">support@thecoffeeshop.com</a> hoặc gọi số <a href="tel:+84123456789">0123 456 789</a>.</p>
        </div>

        <div class="thank-you-actions">
            <a href="/Order/History" class="btn-order-history">Xem đơn hàng của tôi</a>
            <a href="/Product/Menu" class="btn-continue-shopping">Tiếp tục mua sắm</a>
        </div>

        <div class="special-offer">
            <h3>Ưu đãi đặc biệt cho đơn hàng tiếp theo!</h3>
            <p>Sử dụng mã <strong>WELCOME10</strong> để được giảm 10% cho đơn hàng tiếp theo của bạn.</p>
            <p class="offer-expiry">Mã có hiệu lực trong 30 ngày.</p>
        </div>
    </div>
</div>

<style>
    :root {
        --primary: #6b4226;
        --primary-dark: #5a371f;
        --secondary: #a67c52;
        --accent: #d4a762;
        --accent-light: #e9d2b3;
        --light: #f9f5f0;
        --dark: #3c2415;
        --bg: #fcfaf7;
        --text: #333333;
        --text-light: #666666;
        --white: #ffffff;
        --border: #e0e0e0;
        --success: #4caf50;
        --success-dark: #388e3c;
        --success-light: #a5d6a7;
    }

    body {
        background-color: var(--bg);
        font-family: 'Poppins', sans-serif;
        color: var(--text);
    }

    .thank-you-container {
        max-width: 900px;
        margin: 40px auto;
        padding: 0 20px;
    }

    .thank-you-card {
        background-color: var(--white);
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        overflow: hidden;
        padding: 40px;
    }

    .thank-you-header {
        text-align: center;
        margin-bottom: 40px;
    }

    .success-icon {
        font-size: 80px;
        color: var(--success);
        margin-bottom: 20px;
        animation: scaleIn 0.5s ease-out;
    }

    .thank-you-title {
        font-family: 'Playfair Display', serif;
        font-size: 36px;
        color: var(--dark);
        margin-bottom: 15px;
    }

    .thank-you-subtitle {
        color: var(--text-light);
        font-size: 18px;
    }

    .order-details {
        background-color: var(--light);
        border-radius: 15px;
        padding: 30px;
        margin-bottom: 30px;
    }

    .order-info {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
        margin-bottom: 30px;
    }

    .info-item {
        text-align: center;
    }

    .info-label {
        display: block;
        font-size: 14px;
        color: var(--text-light);
        margin-bottom: 5px;
    }

    .info-value {
        font-weight: 600;
        color: var(--dark);
        font-size: 16px;
    }

    .delivery-status {
        margin-top: 20px;
    }

    .status-timeline {
        display: flex;
        justify-content: space-between;
        position: relative;
        margin: 20px 0;
    }

        .status-timeline::before {
            content: '';
            position: absolute;
            top: 30px;
            left: 50px;
            right: 50px;
            height: 4px;
            background-color: var(--border);
            z-index: 1;
        }

    .status-step {
        position: relative;
        z-index: 2;
        text-align: center;
        width: 80px;
    }

    .step-icon {
        width: 60px;
        height: 60px;
        background-color: var(--white);
        border: 3px solid var(--border);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 10px;
        font-size: 24px;
        color: var(--text-light);
        transition: all 0.3s ease;
    }

    .status-step.active .step-icon {
        background-color: var(--success-light);
        border-color: var(--success);
        color: var(--success-dark);
    }

    .step-label {
        font-size: 14px;
        font-weight: 500;
        color: var(--text-light);
    }

    .status-step.active .step-label {
        color: var(--dark);
        font-weight: 600;
    }

    .thank-you-message {
        margin-bottom: 30px;
        line-height: 1.6;
    }

        .thank-you-message p {
            margin-bottom: 15px;
        }

        .thank-you-message a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

            .thank-you-message a:hover {
                color: var(--accent);
                text-decoration: underline;
            }

    .thank-you-actions {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-bottom: 40px;
    }

    .btn-order-history, .btn-continue-shopping {
        display: inline-block;
        padding: 14px 30px;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        text-align: center;
    }

    .btn-order-history {
        background-color: var(--primary);
        color: var(--white);
        box-shadow: 0 5px 15px rgba(107, 66, 38, 0.3);
    }

        .btn-order-history:hover {
            background-color: var(--primary-dark);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(107, 66, 38, 0.4);
        }

    .btn-continue-shopping {
        background-color: transparent;
        color: var(--primary);
        border: 2px solid var(--primary);
    }

        .btn-continue-shopping:hover {
            background-color: var(--primary);
            color: var(--white);
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(107, 66, 38, 0.2);
        }

    .special-offer {
        background: linear-gradient(135deg, var(--accent-light), var(--light));
        border-radius: 15px;
        padding: 25px;
        text-align: center;
        border: 2px dashed var(--accent);
    }

        .special-offer h3 {
            color: var(--primary);
            font-size: 20px;
            margin-bottom: 10px;
        }

        .special-offer p {
            margin-bottom: 5px;
        }

        .special-offer strong {
            background-color: var(--primary);
            color: var(--white);
            padding: 3px 8px;
            border-radius: 5px;
            font-size: 16px;
            letter-spacing: 1px;
        }

    .offer-expiry {
        font-size: 14px;
        color: var(--text-light);
        font-style: italic;
    }

    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Hiệu ứng cho các bước trạng thái
        const steps = document.querySelectorAll('.status-step');

        // Đánh dấu bước đầu tiên là active (đã hoàn thành)
        steps[0].classList.add('active');

        // Hiệu ứng confetti khi trang tải xong
        createConfetti();

        function createConfetti() {
            const confettiCount = 200;
            const container = document.querySelector('.thank-you-container');

            for (let i = 0; i < confettiCount; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';

                // Ngẫu nhiên màu sắc
                const colors = ['#d4a762', '#6b4226', '#a67c52', '#4caf50', '#f9f5f0'];
                const color = colors[Math.floor(Math.random() * colors.length)];

                // Ngẫu nhiên kích thước
                const size = Math.random() * 10 + 5;

                // Ngẫu nhiên vị trí
                const left = Math.random() * 100;

                // Ngẫu nhiên thời gian rơi
                const animationDuration = Math.random() * 3 + 2;

                // Ngẫu nhiên độ trễ
                const animationDelay = Math.random() * 2;

                // Thiết lập style
                confetti.style.backgroundColor = color;
                confetti.style.width = `${size}px`;
                confetti.style.height = `${size}px`;
                confetti.style.left = `${left}%`;
                confetti.style.animationDuration = `${animationDuration}s`;
                confetti.style.animationDelay = `${animationDelay}s`;

                container.appendChild(confetti);

                // Xóa confetti sau khi animation kết thúc
                setTimeout(() => {
                    confetti.remove();
                }, (animationDuration + animationDelay) * 1000);
            }
        }
    });
</script>

<style>
    /* Hiệu ứng confetti */
    .confetti {
        position: absolute;
        top: -20px;
        width: 10px;
        height: 10px;
        background-color: var(--accent);
        opacity: 0.7;
        animation: fall linear forwards;
        z-index: 0;
    }

   

    }
</style>